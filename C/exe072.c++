#include <stdio.h>
#include <locale.h>

enum dir {Leste = 5, Oeste = 10, Norte = 15, Sul = 20};

int main () {

    int direcao = Oeste;
    
        printf("O dir está na pocição %d", direcao);
  
    return 0;
}