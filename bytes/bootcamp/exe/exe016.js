function aplica(numero) {
    return multi(numero)
    }

function multi (n) {
    return n * n
}

console.log(aplica(5))
//console.log(aplica(5,(n) => n* -1))
//console.log(aplica(5,(n) => n % 2 === 0 ? "Par" : "Ìmpar"))

